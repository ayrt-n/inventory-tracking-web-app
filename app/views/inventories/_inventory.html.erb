<tr>
  <td><%= link_to inventory.product.sku, inventory %></td>
  <td><%= inventory.product.name %></td>
  <td><%= inventory.quantity %></td>
  <td><%= link_to "Edit", edit_product_path(inventory.product) %></td>
  <td><%= link_to "Delete", inventory.product, method: :delete, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %></td>
</tr>